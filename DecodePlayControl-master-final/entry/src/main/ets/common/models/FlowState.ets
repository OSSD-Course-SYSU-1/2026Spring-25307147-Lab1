export enum FlowState {
  IDLE = 'idle',
  DISCOVERING = 'discovering',
  READY = 'ready',
  PREPARING = 'preparing',
  TRANSFERRING = 'transferring',
  RESTORING = 'restoring',
  COMPLETED = 'completed',
  FAILED = 'failed'
}
